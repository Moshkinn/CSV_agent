Create and work in (venv)
Create .env with OPENAI_API_KEY
python -m pip install -r requirements.txt

TODO format the requirements.txt file with lib versions

//////////////////////////////////////////////////////////

pip install streamlit langchain openai pandas python-dotenv
